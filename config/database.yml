defaults: &defaults
  adapter: mysql2
  username: root
  password:
  encoding: utf8
  host: 127.0.0.1

development:
  <<: *defaults
  database: memoria_development

test:
  <<: *defaults
  database: memoria_test

production:
  <<: *defaults
  database: memoria_production
